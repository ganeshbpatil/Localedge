apiVersion: v1
kind: Service
metadata:
  name: localedge-web
  namespace: localedge
spec:
  selector:
    app: localedge-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
